.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install texttractor

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv texttractor
    $ pip install texttractor
